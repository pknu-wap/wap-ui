import styled from '@emotion/styled';
import { common } from '../../theme/common';

export const Root = styled.div`
  max-width: 800px;
  padding: 10px 20px;
  background-color: ${common.color.gray2};
`;
